package com.bnpparibas.dsibddf.simulator.common.suggestion;

public interface SuggestionContextResolver<C> {

    /**
     * Résout une variable métier (ex: "grossCapitalGain")
     * à partir du contexte du simulateur.
     */
    Object resolve(String variableName, C context);
}
